import React, { Component } from "react";

class Expanation extends Component {
  render() {
    return (
      <div>
        This is a simple to-do application built with React. You can add tasks,
        mark them as completed, and delete them.
      </div>
    );
  }
}

export default Expanation;
